{
    "model_version": "lstm",
    "lstm_num_units": 50,
    "embedding_size": 50,

    "learning_rate": 1e-3,
    "batch_size": 32,
    "num_epochs": 2,
    "dropout_rate": 0.3,

    "save_summary_steps": 100
}
