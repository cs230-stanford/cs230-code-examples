{
	"model_version": "lstm",
    "lstm_num_units": 200,
    "embedding_size": 50,

    "learning_rate": 1e-3,
    "batch_size": 5,
    "num_epochs": 10,
    "dropout_rate": 0.3,

    "save_summary_steps": 100
}
